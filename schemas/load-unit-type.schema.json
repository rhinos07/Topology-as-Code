{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://warehouse-as-code.internal/schemas/load-unit-type.schema.json",
  "title": "Load Unit Type",
  "description": "Physical definition of a load unit (pallet, container, carton) that can be referenced by movement_rules and replenishment_strategies.",
  "type": "object",
  "required": ["id"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9_]+$",
      "description": "Unique ID, e.g. 'pallet_euro'"
    },
    "description": { "type": "string" },
    "dimensions": {
      "type": "object",
      "description": "Footprint/height of the empty carrier itself (e.g. an empty pallet's own height), used for geometric fit checks against storage_type.default_attributes.size.",
      "properties": {
        "width": { "$ref": "quantity.schema.json#/definitions/length" },
        "depth": { "$ref": "quantity.schema.json#/definitions/length" },
        "height": { "$ref": "quantity.schema.json#/definitions/length" }
      },
      "required": ["width", "depth", "height"],
      "additionalProperties": false
    },
    "max_weight": { "$ref": "quantity.schema.json#/definitions/mass" },
    "stackable": { "type": "boolean" },
    "volume": {
      "$ref": "quantity.schema.json#/definitions/volume",
      "description": "Nominal loaded volume of this load unit type. This can differ from width*depth*height of the empty carrier dimensions."
    }
  },
  "additionalProperties": false
}
