{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://warehouse-as-code.internal/schemas/replenishment-strategy.schema.json",
  "title": "Replenishment Strategy",
  "description": "Replenishment rule. Is process configuration, not physical structure - own lifecycle/review flow.",
  "type": "object",
  "required": ["id", "trigger", "source", "destination"],
  "properties": {
    "id": { "type": "string" },
    "trigger": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["min_max", "quantity_based", "zero_stock", "predictive"]
        },
        "min_qty": { "type": "number" },
        "max_qty": { "type": "number" },
        "check_interval": { "$ref": "quantity.schema.json#/definitions/duration" },
        "forecast_horizon": { "$ref": "quantity.schema.json#/definitions/duration" },
        "based_on": { "type": "string" }
      },
      "additionalProperties": false
    },
    "source": {
      "type": "object",
      "properties": {
        "storage_type": { "type": "string" },
        "section": { "type": "string" }
      },
      "minProperties": 1,
      "additionalProperties": false,
      "dependencies": { "section": ["storage_type"] }
    },
    "destination": {
      "type": "object",
      "properties": {
        "storage_type": { "type": "string" },
        "activity_area": { "type": "string" }
      },
      "minProperties": 1,
      "additionalProperties": false
    },
    "unit_conversion": {
      "type": "object",
      "properties": {
        "from_uom": { "type": "string" },
        "to_uom": { "type": "string" }
      },
      "required": ["from_uom", "to_uom"],
      "additionalProperties": false
    },
    "priority": {
      "type": "string",
      "enum": ["low", "normal", "urgent"]
    },
    "timing": { "type": "string" }
  },
  "additionalProperties": false
}
